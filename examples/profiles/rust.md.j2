{# Sample file-based profile guidance.

   Drop this at `.rosita/templates/profiles/rust.md.j2` (or in the global
   templates dir) and it is used as the guidance for the `rust` profile instead
   of the inline `guidance = "…"` string. It is rendered with the same model as
   the main template, so `context`, `profile`, and `agent` are all available. #}
This is a **Rust** project.

- Build: {% if context.commands.build %}{% for c in context.commands.build %}`{{ c }}`{% if not loop.last %}, {% endif %}{% endfor %}{% else %}`cargo build`{% endif %}
- Test: {% if context.commands.test %}{% for c in context.commands.test %}`{{ c }}`{% if not loop.last %}, {% endif %}{% endfor %}{% else %}`cargo test`{% endif %}
- Lint/format: `cargo clippy --all-targets`, `cargo fmt`

Conventions:
- Prefer `?`/`Result` over `unwrap()`/`expect()` outside tests.
- Keep modules small and add unit tests next to the code they cover.
{% if context.git and context.git.is_worktree %}
> You're in a linked worktree — double-check which checkout your changes land in.
{% endif %}
